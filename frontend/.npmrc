# Ensure optional dependencies are installed
# This fixes issues with platform-specific packages like @rollup/rollup-linux-x64-gnu
optional=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=false

# Strict SSL
strict-ssl=true
